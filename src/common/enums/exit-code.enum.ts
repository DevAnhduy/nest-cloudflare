export enum AuthenticationExitCode {
  NO_TOKEN = 1001,
  INVALID_TOKEN = 1002,
  EXPIRED_TOKEN = 1003,
}

export enum AuthorizationExitCode {
  ACCESS_DENIED = 2001,
}

export enum ValidationExitCode {
  MISSING_FIELD = 3001,
  INVALID_FORMAT = 3002,
  UNIQUE_VALUE = 3003,
  INVALID_LENGTH = 3004,
  INVALID_CHARACTOR = 3005,
  NOT_FOUND = 3006,
  ERROR = 3007,
}

export enum DatabaseExitCode {
  CANNOT_CONNECT = 4001,
  UNAUTHORIZE = 4002,
  TIMEOUT = 4003,
  OPERATOR_ERROR = 4004,
  LOST_CONNECT = 4005,
  UNIQUE_FIELD_VALUE = 4006,
  UNKNOW_VALUE = 4007,
}

export enum FileExitCode {
  INVALID_EXTENSION = 5001,
  INVALID_SIZE = 5002,
  INVALID_NAME = 5003,
  MAXIMUM_FILE = 5004,
  FULL_DISK = 5005,
  PERMISSION_ERROR = 5006,
}

export enum ServerExitCode {
  INTERNAL_SERVER_ERROR = 6001,
}

export enum UnknowExitCode {
  UNKNOW_ERROR = 9001,
}
